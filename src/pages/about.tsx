import React from 'react';

import Layout from '../components/Layout';

export default () => {
    return (
        <div>
            <Layout> 
                <p>This is layout for about page </p>
            </Layout>
        </div>
    );
}